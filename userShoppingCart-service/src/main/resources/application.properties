
#server config 
server.port = 9009
spring.application.name=userShoppingCart-service


# servlet contenxt path for global request mapping
server.servlet.context-path=/api/userShoppingCart-service

# eureka config
eureka.client.serviceUrl.defaultZone = http://localhost:9001/eureka

#instance config
eureka.instance.hostname=localhost
