#server config 
server.port = 9008
spring.application.name=userAccounts-service


# servlet contenxt path for global request mapping
server.servlet.context-path=/api/userAccounts-service

# eureka config
eureka.client.serviceUrl.defaultZone = http://localhost:9001/eureka

#instance config
eureka.instance.hostname=localhost